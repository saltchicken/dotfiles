#### Packages to Install

kitty  

rust  

`
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
rustup update
`
yazi (install via rust)--
`
cargo install --locked yazi-fm yazi-cli
`
or
`
cargo install --locked --git https://github.com/sxyazi/yazi.git yazi-fm yazi-cli
`

keyd
