monitor.alsa.rules = [
  {
    # Match the specific device by its name
    matches = [
      {
        device.name = "alsa_card.pci-0000_01_00.1"
      }
    ]
    # Apply this action
    actions = {
      # This disables the device entirely
      update-props = {
        device.disabled = true
      }
    }
  }
]
